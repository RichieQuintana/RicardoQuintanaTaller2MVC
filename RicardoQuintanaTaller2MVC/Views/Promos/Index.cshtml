@model IEnumerable<RicardoQuintanaTaller2MVC.Models.Promo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PromoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PromoName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PromoDescripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PromoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PromoName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PromoDescripcion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
